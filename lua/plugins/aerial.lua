return {
  "stevearc/aerial.nvim",
  opts = {
    -- Aerial's own setup options
    layout = {
      -- You can specify an integer or a float (relative to the editor width)
      width = 30,
      min_width = 30,
      max_width = 60,
      -- Choose where to place the window, etc.
      default_direction = "prefer_right",
    },
    -- Other Aerial settings...
  },
}
